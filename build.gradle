plugins {
    id 'java'
}

group = 'guru.qa'

java {
    sourceCompatibility = '17'
}

subprojects {
    repositories {
        mavenCentral()
    }
    ext {
        postgresDriverVersion = '42.6.0'
        flywayVersion = '9.22.2'
        lombokVersion = '1.18.30'
    }
}

tasks.named('test') {
    useJUnitPlatform()
}